Currently, I have all of my packages included in a single packages.nix file
under "modules/". I'm not sure if this is a clean approach. Maybe I should
divide those imports based on category in separate files together with the other
.nix files of the same category?
